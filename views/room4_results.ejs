<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Salle 4 – Résultats</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <section class="card">
      <h2>Salle 4 – Résultats</h2>
      <p>Requête SQL exécutée (vulnérable) :</p>
      <pre><%= sql %></pre>

      <% if (rows.length === 0) { %>
        <p>Aucun grimoire trouvé.</p>
      <% } else { %>
        <ul>
          <% rows.forEach(r => { %>
            <li>
              <strong><%= r.title %></strong><br>
              <em>Note secrète :</em> <%= r.secret_note %>
            </li>
          <% }); %>
        </ul>
      <% } %>

      <p><a href="/room4">Retour à la salle 4</a></p>
    </section>
  </main>
<%- include('partials/footer') %>
</body>
</html>
